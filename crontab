# veo-audit cron schedule, read by supercronic in the `scheduler` container.
# Times are in America/Denver (set via TZ env on the scheduler container) so
# the daily SLA window correctly tracks DST. The 2-min ingest cadence drifts
# by 60 minutes twice a year at DST transitions — acceptable for a compliance
# audit; missed cycles are caught by the staleness check on the next firing.

# Ingest: every 2 minutes (cadence cutover 2026-07-07; was */10 since v3.0).
# The upstream feed is generated per-request (last_updated stamps at fetch
# time, ttl=0), so any cadence returns current fleet state; 2 min tightens
# trip-duration resolution to ±2 min for the burn-rate work. NOTE: the live
# schedule is the admin-editable copy on the scheduler_state volume
# (/app/state/crontab) — this file only seeds fresh environments. Changing
# cadence in prod = edit /admin/scheduler/edit to match.
*/2 * * * * cd /app && python -m src.cli ingest_cycle

# Daily SLA window: 9:00 AM Denver, DST-aware. Computes the 6-9 AM window
# that just closed. Idempotent via ON CONFLICT.
0 9 * * * cd /app && python -m src.cli daily_sla

# Daily trip/popularity rollup: same 9:00 AM slot, but rolls up
# YESTERDAY's full calendar day (today isn't over yet). Separate command
# from daily_sla so a failure in one doesn't block or mask the other.
0 9 * * * cd /app && python -m src.cli daily_trips

# Equity Area compliance reprocessing (sql/079 / src/equity_backfill.py).
# The city clarified WHICH map the Veo contract means in August 2026, so
# every snapshot recorded before that deploy has NULL in every `*_equity`
# column and the contractual series would otherwise only ever fill forward.
# This rebuilds prior days from device_history's stop intervals -- the only
# durable record of where the fleet was, now that raw_telemetry_points is
# flushed to R2 after 24 hours.
#
# 9:40 AM, deliberately AFTER the 9:00 daily_sla / daily_trips pair and the
# 9:15 area-leader recompute: it re-averages daily_sla rows that the 9:00
# job may have only just written, so running first would just make it
# re-run them tomorrow.
#
# Self-limiting and idempotent: it only picks up days whose SLA row still
# has no equity figure, within a 14-day lookback, so steady state is a
# no-op and a deep backlog drains over a few nights instead of one long
# run. `equity_backfill <start> [end]` does an explicit range by hand.
40 9 * * * cd /app && python -m src.cli reprocess_equity_compliance

# Archive: fires daily at 02:00 AM Denver but only acts if >= archive_hours
# (config.json; 24 since the 2-min cadence cutover — a 48h window at 2-min
# is ~12.5M rows, too close to the archive job's memory ceiling) elapsed
# since the last successful upload. Self-gating means we can run this on a
# daily cron without forcing an exact-interval cron expression.
0 2 * * * cd /app && python -m src.cli archive_if_due

# Receipt retention: purge discount-report receipt images older than 18
# months (privacy policy). Idempotent; the report row outlives the image.
30 3 * * * cd /app && python -m src.cli cleanup_receipts

# Ride-watch expiry: close out user_device_watch_list / tracked_rides rows
# whose 3h window elapsed with no GBFS-side resolution (src/ride_watch.py
# handles the two live transitions every 2-min cycle; this just terminates
# the ones that timed out). Cheap indexed range scan — no self-gating
# needed like archive_if_due.
*/15 * * * * cd /app && python -m src.cli expire_stale_watches

# Off-feed ride expiry: close out `rides` rows left 'active' for 24 hours
# with no end report (sql/040). Shares this slot with the watch expiry above
# because it is the same kind of job, but it is NOT cosmetic like that one:
# idx_rides_one_active_per_account is a partial UNIQUE index on
# status = 'active', so an abandoned ride 409s its owner out of
# POST /api/v1/rides/start until this runs. Same cheap partial-index scan.
*/15 * * * * cd /app && python -m src.cli expire_stale_off_feed_rides

# Ride transaction screenshot retention: mirrors cleanup_receipts' 18-month
# window and slot in the same daily 3:30am run.
30 3 * * * cd /app && python -m src.cli cleanup_ride_screenshots

# Job-run ledger retention (sql/062): drop /admin/scheduler's run rows older
# than 30 days. Shares the 3:30am retention slot with the sweeps around it.
# The table is small -- every command except the 2-minute ingest cycle, so
# roughly 700 rows a day -- but it is append-only and nothing else would ever
# bound it.
30 3 * * * cd /app && python -m src.cli cleanup_job_runs

# Model-report photo retention: same private bucket, same 18-month window
# and same 3:30am slot as the receipts job above. sql/038 added
# photo_deleted_at and nothing set it, so these objects were retained
# forever despite the published policy. Stamps the row; keeps the report.
30 3 * * * cd /app && python -m src.cli cleanup_model_report_photos

# Routing graph refresh. denver-map-prep rebuilds and re-uploads the .pbf at
# 02:00 Denver daily; this picks it up afterwards, once map-prep's ~50-minute
# window has closed. Downloads only when the R2 ETag changed, so most days it
# is a no-op. A changed .pbf is logged loudly: the valhalla container has to be
# recreated to rebuild tiles, and this container deliberately has no Docker
# socket to do that itself.
30 4 * * * cd /app && python -m src.cli refresh_routing_graph

# Photon geocoding index refresh. The index itself is rebuilt BY HAND, roughly
# quarterly (scripts/build_photon_index.md), so this is only an R2 ETag check
# and a no-op on all but ~4 days a year. 05:00 keeps it clear of the 04:30
# routing-graph refresh: both unpack multi-GB artifacts into volumes on the
# same disk. A changed index is logged loudly, because the photon container has
# to be RESTARTED to load it (Photon maps the index at JVM startup) and this
# container deliberately has no Docker socket to do that itself — the same
# limitation as refresh_routing_graph above.
0 5 * * * cd /app && python -m src.cli refresh_photon_index

# Denver address index (sql/074). ~413k points over ~207 paginated requests
# against the city's open-data service, so WEEKLY rather than nightly: a
# municipal address file gains a few hundred rows a month, and hammering
# someone else's public endpoint nightly for that is rude as well as
# pointless. Sunday 04:15, clear of the 02:00 archive and the 05:00 photon
# refresh so three heavy jobs never overlap.
#
# Loads into scratch tables and swaps in one transaction, so a failed or
# half-finished run leaves the previous index serving rather than answering
# confidently from whatever landed. Freshness is stamped into system_state
# under address_points_refreshed_at.
15 4 * * 0 cd /app && python -m src.cli refresh_address_points

# Battery model. Reads the last ~26h of raw_telemetry_points, so it MUST run
# BEFORE the 02:00 archive, which flushes that table to R2 and truncates it.
# Running after would see only the handful of cycles since the truncate. 01:30
# leaves the full day in place; the 26h lookback overlaps the previous
# truncation boundary so nothing falls between two runs.
# Yields ~120 observations/day; the model becomes trainable after ~2 weeks.
# To seed it from history instead, see `backfill_battery_trips` (manual, needs
# a raised memory limit).
30 1 * * * cd /app && python -m src.cli extract_battery_trips

# Refit weekly. Runs after extraction has had its turn, and the coefficients
# move slowly enough that a daily refit would mostly chase weather noise.
45 5 * * 1 cd /app && python -m src.cli train_battery_model

# Inbound SMS replies from z280-comms. Replies are polled, never pushed, and
# the poll CLAIMS what it returns — nothing is redelivered — so this is the
# only thing that will ever see a rider's STOP. Every 5 minutes: consent is
# enforced upstream at send time regardless, so the only cost of lag here is
# how long our own UI keeps saying "we'll text you" to someone who opted out.
*/5 * * * * cd /app && python -m src.cli poll_comms_replies

# De-id sweep: severs account linkage on donated ride tracks
# (track_donations/donated_track_points -- RIDE_MODE_OVERHAUL_PLAN.md's
# "De-id" glossary entry / PLAN_RIDE_MODE_API.md phase A2) 4h after a
# ride's points settle, with a hard floor of 28h after donation even if
# points never settle -- so a donated track is never account-linked past
# 28h. Also carries a to_regclass('ride_routes')-guarded arm for phase
# A3's route geometry (sql/052) that is a safe no-op until that table
# exists, so this line is correct regardless of A2/A3 landing order.
# Hourly at :15 -- cheap, idempotent (WHERE deidentified_at IS NULL),
# safe to run every hour forever.
15 * * * * cd /app && python -m src.cli deidentify_donations

# H3 r8 cell universe (FEATURE_PLAN_2026-07.md §11 / PLAN_RIDE_MODE_API.md
# phase A4): full-replaces the list of cells that have EVER had an observed
# device or a point — all-time, not windowed (src/area_leaders.py:
# refresh_universe).
#
# WEEKLY, not daily, since sql/061. This used to compute the whole
# leaderboard nightly; now the leaderboard is computed per request and this
# job keeps only the half that cannot be: the all-time universe, whose one
# expensive step is a DISTINCT scan of ~7.3M unindexed device_history rows.
# An all-time answer barely moves week to week, and the read path unions
# this list with whatever has points in the window anyway — so a cell that
# saw its first device on Tuesday still renders on Tuesday, not next Monday.
#
# Monday 09:15 keeps the old slot's reasoning: the 9:00 AM Denver rollups
# (daily_sla/daily_trips) are above, and :15 keeps this off their seq-scan
# I/O rather than contending for the same few seconds.
15 9 * * 1 cd /app && python -m src.cli refresh_area_universe

# Device feature confirmations (sql/055 / src/device_features.py). Grades
# whatever riders submitted since the last firing: the first valid report on
# a vehicle becomes authoritative, a later report that disagrees flags it
# 'needs review', and three reports resolve a review by 2/3 consensus.
#
# "Every ten minutes on the 8s" is the owner's cadence, spelled out
# literally: :08, :18, :28, :38, :48, :58. The offset is the point — every
# other 10-minute-ish job in this file sits on :00 or :15, and the grading
# pass reads device_state rows the 2-minute ingest cycle writes, so landing
# on :08 keeps it clear of the top-of-the-minute ingest burst and of the
# :15 watch/off-feed expiries.
#
# Idempotent by construction: a report is claimed by stamping processed_at
# in the same transaction that writes the state it produced, so a firing
# that overlaps a slow predecessor re-grades nothing.
8,18,28,38,48,58 * * * * cd /app && python -m src.cli process_device_feature_reports

# User-analytics daily rollup (sql/061 / src/analytics.py): recomputes
# YESTERDAY's telemetry_daily + request_metrics_daily (Denver calendar day,
# same convention as daily_trips). 9:20 keeps it clear of the 9:00
# sla/trips pair and the 9:15 area-leader recompute — same morning rollup
# family, its own slot. Idempotent delete-and-reinsert per day.
20 9 * * * cd /app && python -m src.cli rollup_analytics

# User-analytics retention (privacy policy / src/api_meta.py _PRIVACY):
# raw telemetry_events 90d, raw request_metrics 30d, telemetry_salt 2d —
# destroying the salt is what makes visitor_hash irreversible. Joins the
# 3:30 retention family at its own :45 slot.
45 3 * * * cd /app && python -m src.cli cleanup_telemetry
