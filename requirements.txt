fastapi==0.115.5
uvicorn[standard]==0.32.1
psycopg[binary,pool]==3.2.3
duckdb==1.1.3
httpx==0.27.2
pydantic==2.9.2
jinja2==3.1.4
authlib==1.3.2
itsdangerous==2.2.0
python-multipart==0.0.12
sentry-sdk[fastapi]==2.18.0
boto3==1.35.65
pyarrow==18.1.0
h3==4.1.2
pillow==11.0.0
# Unpacking the Photon geocoding index (photon-index-<date>.tar.zst) in
# src/r2_map.py: stdlib tarfile reads only gz/bz2/xz and the worker image ships
# no zstd binary. Imported lazily, so src.cli stays importable without it.
zstandard==0.25.0

